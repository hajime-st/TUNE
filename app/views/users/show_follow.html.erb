<%#= link_to "view my profile", user_path(@user.username) %>
<%#= render 'shared/stats' %>
<%# if @users.any? %>
  <%#= render @users %>
<%# end %>

<div class="flex flex-col mx-auto border items-center w-1/2 my-5 h-screen">
  <div class="self-start text-3xl font-extrabold p-2"><%= "@#{@user.username}" %></div>
  <%= render 'shared/detail_follow_stats' %>
  <% if @users.any? %>
    <%= render @users %>
  <% end %>
</div>