<% if @themes.present? %>
  <%#= turbo_frame_tag @themes.current_page do %>
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6 mt-10 w-11/12 mx-auto">
      <%= render @themes %>
    </div>
    <div class="flex justify-center my-10">
      <%= paginate @themes %>
    </div>
      <%#= turbo_frame_tag @themes.next_page, loading: :lazy, src: path_to_next_page(@themes) %>
  <%# <% end %>
<% else %>
  <p><%= 'お題がありません' %></p>
<% end %>