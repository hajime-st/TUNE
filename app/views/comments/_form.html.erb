<div>
  <%= form_with model: comment, url: [theme, comment], local: true do |f| %>
    <%= f.label :body %><br>
    <%= f.text_area :body, row: 4, placeholder: Comment.human_attribute_name(:body) %>
    <%= f.submit t('defaults.post') %>
  <% end %>
</div>